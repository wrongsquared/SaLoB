apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ai-service
  template:
    metadata:
      labels:
        app: ai-service
    spec:
      containers:
        - name: ai-service-container
          image: ai-service:1.0 # From our local docker registry
          imagePullPolicy: Never # CRITICAL: Don't look online for this image
          ports: # Listening on
            - containerPort: 8080
            - containerPort: 9090
          envFrom:
            - secretRef:
                name: ai-service-secrets # Refers to kustomization yaml
